# Frontend Environment Variables
# Copy this file to .env.local to override values during development

# Optional: override the API base URL
# Defaults to '/api' which is proxied by Vite (dev) or Nginx (production)
# Only needed if your backend is hosted on a different domain/port
# VITE_API_URL=http://localhost:8000/api
